## 1. Primary-Role Core

- [x] 1.1 Add minimal, testable primary-target selection and origin-translation logic beside `DisplayMonitor`, covering configured hub present, configured hub absent with an active built-in display, clamshell with an inactive built-in display, missing display signals, and no-op targets; verify the focused display-role tests pass.
- [x] 1.2 Implement one idempotent CoreGraphics transaction that translates all active display origins so the selected target is at `(0,0)`, completes with application-only scope, verifies the resulting primary display, and does not call mode, enablement, or mirroring APIs; verify no-op and successful/failing transaction paths through the display configuration seam and `swift test`.

## 2. Signal And Lifecycle Integration

- [x] 2.1 Wire the existing stable USB-hub condition to primary-role synchronization at startup and on hub transitions while leaving `OwnershipController` keyboard decisions and manual actions unchanged; verify existing ownership tests still pass and manual claim/release does not request a display-role change.
- [x] 2.2 Re-evaluate primary-role eligibility when the selected display becomes available, the built-in display changes active state, or the Mac wakes, while suppressing changes during sleep and guarding against callbacks generated by the app's own display transaction; verify lid-open/lid-closed and repeated-notification cases without a synchronization loop.

## 3. Recovery, Documentation, And Validation

- [x] 3.1 Keep primary-display failures best-effort by logging the failure and allowing the keyboard operation to continue independently; verify a simulated display-configuration failure does not prevent keyboard claim or release.
- [x] 3.2 Update `README.md` and the settings display-behavior text to describe hub-driven primary selection, application-scoped behavior, clamshell no-op handling, and macOS-owned window relocation; verify no stale display-neutral or mirroring/parking instructions remain.
- [x] 3.3 Run `swift build`, `swift test`, and `bash scripts/run-checks.sh`, then manually validate both hub directions with the lid open and closed, including primary-display verification, normal macOS window relocation, unchanged display modes/arrangement, sleep/wake, and application termination behavior.
